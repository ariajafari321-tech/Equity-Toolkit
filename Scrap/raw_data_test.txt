import sys, pathlib

ROOT = pathlib.Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))

from src.data import load_raw, to_wide, clean_prices
from src.analysis import log_returns, rolling_volatility

px = clean_prices(to_wide(load_raw(ROOT / "data" / "raw" / "prices.csv")))
r = log_returns(px)
vol = rolling_volatility(r, window=21)

print("returns:", r.shape)
print("vol:    ", vol.shape)
print()
print("NVDA annualized vol:")
print(vol["NVDA"].dropna().describe())
print()
print("KO annualized vol:")
print(vol["KO"].dropna().describe())